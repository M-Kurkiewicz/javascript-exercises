const repeatString = function(text,count) {
    let finalText = "";
    for(i=0;i<count;i++) {
        finalText += text;
    }
    return finalText
};

// Do not edit below this line
module.exports = repeatString;
